# 개발 설치는 dev-install.bat (또는 아래) 를 쓴다 - 3개 패키지를 editable 로 설치한다:
#   python -m pip install hatchling
#   python -m pip install --no-build-isolation -e packages/mail-core -e packages/mail-app -e packages/admin-ui
#
# 런타임 의존성:
#   mail-core  : cryptography (AES-GCM 대칭 암호화, mail_core.crypto)
#   mail-app   : 없음 (형제 mail-core 필요)
#   admin-ui   : flask  (로컬 관리 웹 UI 전용 예외)
flask
cryptography>=42
